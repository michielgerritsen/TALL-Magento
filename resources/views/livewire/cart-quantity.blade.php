<div>
    @if ($quantity)
        <div class="rounded-full bg-gray-600 w-5 h-5 text-white text-xs -pt-2 -ml-5 -mt-4">
            <span class="-mt-1 block">
                {{ $quantity }}
            </span>
        </div>
    @endif
</div>
